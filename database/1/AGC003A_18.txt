61 62 0.520000 fixed
else output No
44 47 0.500000
declare string variable s, input s, declare int variable a equals 0, b equals 0, iterate i from 0 to size of s, each time plus 1
58 59 0.500000 fixed
if a equals 0 and b equals 0, output Yes
64 64 0.500000
return 0
48 55 0.481481 fixed
if s[i] equals N, a plus 1, else if s[i] equals S, a minus 1, if s[i] equals E, b plus 1, else if s[i] equals W, b minus 1
1 43 0.000000
include headerfiles, define unsigned long long as ull, define long long as ll, define 90001 as mod, define 0x3f3f3f3f as INF, define 10010 as maxn, define memset(a,0,size of a) as cle(a), declare constant unsigned long long variable equals 1LL left move 61 bit, declare constant double variable eps as 1e minus 5, specify namespace, declare priority queue of int, vector of int, and greater of int pq, struct Node with int variables x, y, struct cmp with int parameters a,b, return a larger than b, int main function, if define online judge, end if
56 57 0.000000

60 60 0.000000

63 63 0.000000

65 65 0.000000

