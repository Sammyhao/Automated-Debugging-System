 your communities  Find centralized, trusted content and collaborate around the technologies you use most.  Connect and share knowledge within a single location that is structured and easy to search.  I am trying to solve this problem http://www.spoj.com/problems/TSORT/ but I receive this error time limit exceeded. My code compiles and sorts correctly on my computer, but when I submit to the spoj it's return this error.  the posted code does not compile! Amongst other things, it is missing the needed statements for the needed header files. Do you expect us to guess as to which header files your code actually includes?  the SPOJ will supply the inputs via stdin. so the function: can be completely eliminated as can the header file:  1 Answer 1  uses the 'fastRead()' function rather than 'scanf()'  uses the 'fastWrite()' function rather than 'printf()'  uses 3 exclusive-or statements (as a macro) rather than the 'swap()' function  sets functions 'inline' when the compiler allows it  uses the 'getchar_unlocked()', 'putchar_unlocked()' from stdio.h rather than the much slower 'getchar(), and 'putchar()'  and now the proposed code:  to eliminate any duplicate number output this code block:  By clicking “Accept all cookies”, you agree Stack Exchange can store cookies on your device and disclose information in accordance with our Cookie Policy.  