 your communities  Find centralized, trusted content and collaborate around the technologies you use most.  Connect and share knowledge within a single location that is structured and easy to search.  I am trying to write myself the isEmpty method for a binary tree but I am having a problem. So this is the method that I am using.  When I add only one element, and then remove this element, and call isEmpty, I do not get true, but false.  Is there somethng wrong with my implementation?  So this is the remove method:  and this is the removeMin method that the remove method uses:  The method should really look like this, although it is only good practice: public boolean isEmpty() { return root == null; }  2 Answers 2  Check your remove element code. Usually than code find out parent of remove node and set to null corresponding reference. And for last element it have to set to null variable.  Your method searchs the node with X element and replace it by one of its children with the method (in case it has 2 children) or with the left or the right child node. Your public remove method could be something like this:  83k1515 gold badges147147 silver badges312312 bronze badges  2  still does not work. when I try to remove the element entered first, the isEmpty still is false, which means that the root is not set to null yet.  have you made some debug and checked that root and node have the same value when your tree has only 1 element?  By clicking “Accept all cookies”, you agree Stack Exchange can store cookies on your device and disclose information in accordance with our Cookie Policy.  