 your communities  Find centralized, trusted content and collaborate around the technologies you use most.  Connect and share knowledge within a single location that is structured and easy to search.  There was no output and no error, I don't know how to iterate through this Database map, I attempted at DatabaseTest but it didn't work out.  What I want to do is iterate through the database id and retrieve the value object then separate the value object up into e.g. fishName, description, etc. and assign each variable to them for DatabaseTest class.  @nonope Your class is what initializes the database with content, but in your test class you don't create a instance so the database will be empty. You don't do anything with the values in the loop, so even if the db isn't empty you wouldn't get any output besides the failure message.  1 Answer 1  In your test you never populate the database map with anything. So if it's empty your loop will not run.  If you actually use your FishServ class, then you could just get the list that you are returning in and iterate over that like so:  By clicking “Accept all cookies”, you agree Stack Exchange can store cookies on your device and disclose information in accordance with our Cookie Policy.  