 your communities  Find centralized, trusted content and collaborate around the technologies you use most.  Connect and share knowledge within a single location that is structured and easy to search.  I have the below code for tree map where I store duplicate key and it seems overwrite the existing one.  It prints So it means map allow to overwrite on key basis?  An object that maps keys to values. A map cannot contain duplicate keys; each key can map to at most one value. - The very first line of the Map interface.  The very purpose of a map is to represent a series of key/value entries where each key is unique...  This question appears to be off-topic because it doesn't show minimal understanding of the question being asked.  Yes, It silently overrides. One have one only. That is the latest inserted value with that key.  2 Answers 2  All maps share the same basic properties, one of which is that all keys must be unique. Hence why returns a .  To do what you are looking for you need a Multimap - which is essentially a Map to a List.  To add an object get the list for that key, if it is null add a list then add your value to the list, otherwise just add your value to the existing list.  There are some multimap implementation available in various 3rd party libraries or it's easy enough to implement your own.  Associates the specified value with the specified key in this map. If the map previously contained a mapping for the key, the old value is replaced.  By clicking “Accept all cookies”, you agree Stack Exchange can store cookies on your device and disclose information in accordance with our Cookie Policy.  