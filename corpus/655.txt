 your communities  Find centralized, trusted content and collaborate around the technologies you use most.  Connect and share knowledge within a single location that is structured and easy to search.  If I make a tree using networkx and draw it, the nodes overlap. Is there a way to draw it so there is no overlap?  2 Answers 2  I am no expert in this, but here is code that uses the library and its dependency. These libraries come with Anaconda Python but are not installed by default, so first do this from the command prompt:  If you adjust the window to make it square, the result is  Or, if you prefer a top-down tree, you could replace the string in that code with , and if you make the resulting window wider you get  Also, if you use the string instead and make the window wider, you get  If you want to do this without additional libraries, look at this answer, which shows a way to get a hierarchical tree layout or a circular layout purely in networkx:  I'm planning to add a slightly modified version of this to networkx sometime soon.  By clicking “Accept all cookies”, you agree Stack Exchange can store cookies on your device and disclose information in accordance with our Cookie Policy.  