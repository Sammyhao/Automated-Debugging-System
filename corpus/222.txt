 your communities  Find centralized, trusted content and collaborate around the technologies you use most.  Connect and share knowledge within a single location that is structured and easy to search.  I have a DTO in which I have a field which is an array of numbers. These ids are coming via API query parameters. I am using Class Transformer to transform these ids into an array of numbers. But I am getting an array of strings only. My DTO class is as below.  My Controller code looks like this.  If I call my controller like this below, I am still getting in my locations field.  1 Answer 1  Query parameters always come in as strings, as do URL parameters if you use those. The is enough to modify this though, but you need to make sure you have set in your .  28.8k44 gold badges5757 silver badges7979 bronze badges  1  Thanks a lot Jay! I found the issue after you explained this stuff. I was missing a validation Pipe in my controller.  By clicking “Accept all cookies”, you agree Stack Exchange can store cookies on your device and disclose information in accordance with our Cookie Policy.  