 your communities  Find centralized, trusted content and collaborate around the technologies you use most.  Connect and share knowledge within a single location that is structured and easy to search.  please help me with my error can't seem to make it work because of that can only iterate over an array or an instance of java.lang.Iterable. i want to create a barcode and read it and add it to the word document  Update Post the nodeCollection is from the com.aspose.words.  Simple: don't use a for-each loop here but rather either a standard for loop or a while loop.  The error message is correct. The shorthand notation only works for arrays or s. If your class doesn't implement Iterable, explicitly ask it for its iterator and use that; if it doesn't have a or equivalent method, write an explicit loop to fetch its contents.  -- then the first thing you should do, before even coming here, is to look up the documentation for this class. This should be a reflex action.  The loop fails unless the base class is an instance of a or .  Check if is a collection type class that implemented .  Edit:  the nodeCollection is from the com.aspose.words.  implements generic directly, without specifying the type of objects it would be handling. Hence you should explicitly generate the from the instance and on that you can iterate.  By clicking “Accept all cookies”, you agree Stack Exchange can store cookies on your device and disclose information in accordance with our Cookie Policy.  