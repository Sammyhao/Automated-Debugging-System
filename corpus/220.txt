 your communities  Find centralized, trusted content and collaborate around the technologies you use most.  Connect and share knowledge within a single location that is structured and easy to search.  When I have a template class which contains template map and a declared as in the following code by , how can I iterate through the elements of the map outside the class, f.e in main to print them on the output?  Update: Can the typedef iterator be used outside the class? If yes in what way?  8,3622424 gold badges102102 silver badges163163 bronze badges  1  Your main function is wrong. The complete type would be, for example, . Then you can just say to name your iterator. You also have to expose the iterator functions of in the public interface of .  3 Answers 3  You need to define member functions on your template that would return iterators:  Then:  However, I am unsure what your adding to here, why not just use it as is?  Add begin() and end() members to template_Map (both const and non-const variants).  I added non-const versions just for interface completeness, it may not be required in your case. Than in main:  I don't understand why you want to wrap a class with another one. Why don't you itself?  By clicking “Accept all cookies”, you agree Stack Exchange can store cookies on your device and disclose information in accordance with our Cookie Policy.  